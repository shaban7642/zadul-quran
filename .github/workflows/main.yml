name: Deploy to Production

on:
  push:
    branches: [main] 

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Execute deployment commands via SSH
      uses: appleboy/ssh-action@v1.1.3
      with:
        host: 195.35.9.128
        username: root
        password: 123Zad@zad123
        script: |
          cd /home/projects/zadul-quran
          touch hi.txt
          echo "its working" > hi.txt