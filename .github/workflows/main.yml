on:
  push:
    branches:
      main
jobs:
  deploy_job:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install sshpass
        run: sudo apt-get install -y sshpass

      - name: Deploy file
        run: |
          sshpass -p '123456Zz' sftp -oStrictHostKeyChecking=no -P 22 root_admin@195.35.9.128:/home/projects/zadul-quran/
